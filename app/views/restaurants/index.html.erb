<h1>Restaurants</h1>

<%=link_to "Create  new  Restaurant", new_restaurant_path %> 

<table>
  <tr>
    <th> Id</th>
    <th>Restaurant Name</th>
  </tr>
  <tbody>
​    <% @restaurant.each do |restaurant| %>
      <tr>
         <td><%= restaurant.id %></td>
         <td><%= link_to restaurant.name, restaurant_path(restaurant) %></td>
         <td><%= link_to "edit", edit_restaurant_path(restaurant)%>  <%= link_to "delete", restaurant_path(restaurant), method: :delete, data: {confirm: "Are you sure you wanna delete this?"} %>
         </td> <br>
      </tr>

  <% end %>

 </tbody>
</table>


 